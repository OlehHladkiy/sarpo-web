export enum ConferenceType {
  Venue = 'venue',
  Online = 'online',
}

export enum ConferenceDateType {
  Single = 'single',
  Multiple = 'multiple',
}

export enum VenueConferenceOnboardingStep {
  Basic = 'basic',
  Details = 'details',
  Tickets = 'tickets',
}
